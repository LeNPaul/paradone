# Server Configuration

## Install paradone in Ubuntu server for the first time

1. Follow steps in `user.sh` and log into the created user.

2. Clone git repository.

3. Run `bash install.sh` (`initiate.sh` is called from `install.sh`).

4. Run `webserver.sh`
